# Database connection string
# Format: "postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME"
# USERNAME and PASSWORD must match the values set in your root .env file
DATABASE_URL = 'postgresql://USERNAME:PASSWORD@db:5432/budgetapp'

# Server port
PORT = 3000
